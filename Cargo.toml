[workspace]
resolver = "2"
members = [
    "crates/crypto",
    "crates/repo",
    "crates/share-protocol",
    "crates/core",
    "apps/share-backend",
]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/stephane-segning/opfs-webauthn"
homepage = "https://github.com/stephane-segning/opfs-webauthn"
authors = ["Stephane Segning <stephane.segning@gmail.com>"]
rust-version = "1.85"

[workspace.lints.rust]
unsafe_code = "forbid"
unreachable_pub = "warn"
missing_debug_implementations = "warn"

[workspace.lints.clippy]
pedantic = { level = "warn", priority = -1 }
nursery = { level = "warn", priority = -1 }
module_name_repetitions = "allow"
must_use_candidate = "allow"
missing_errors_doc = "allow"
missing_panics_doc = "allow"

[workspace.dependencies]
# Crypto
aes-gcm = { version = "0.10", default-features = false, features = ["aes", "alloc"] }
hkdf = { version = "0.12", default-features = false }
sha2 = { version = "0.10", default-features = false }
blake3 = { version = "1.5", default-features = false }
rand_core = { version = "0.6", default-features = false }
zeroize = { version = "1.8", features = ["derive"] }
subtle = { version = "2.5", default-features = false }
base32 = { version = "0.5", default-features = false }
x25519-dalek = { version = "2.0", default-features = false, features = ["alloc", "precomputed-tables", "static_secrets", "zeroize"] }

# Serialization (no_std-friendly: keep default features off)
serde = { version = "1", default-features = false, features = ["derive", "alloc"] }
serde_bytes = { version = "0.11", default-features = false, features = ["alloc"] }
ciborium = { version = "0.2", default-features = false }

# Errors
thiserror = { version = "2.0", default-features = false }

# Dev / test
hex-literal = "0.4"
hex = "0.4"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true

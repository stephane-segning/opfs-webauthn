{
	"name": "@opfs/share-backend",
	"version": "0.1.0",
	"private": true,
	"description": "Cloudflare Worker — recipient-first rendezvous backend for OPFS-WebAuthn share flow (ADR 0007).",
	"license": "MIT",
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"check": "biome check .",
		"check:write": "biome check --write .",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"test": "vitest run",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@noble/hashes": "^1.5.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.2.5",
		"@cloudflare/workers-types": "^4.20251101.0",
		"typescript": "^5.8.2",
		"vitest": "^2.1.0",
		"wrangler": "^3.99.0"
	}
}

{
	"name": "opfs-webauthn",
	"version": "0.1.0",
	"private": true,
	"description": "Local-first PWA notes app — WebAuthn PRF + OPFS-backed SQLite + Rust/WASM crypto.",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"build": "turbo run build",
		"check": "biome check .",
		"check:write": "biome check --write .",
		"dev": "turbo run dev",
		"test": "turbo run test",
		"typecheck": "turbo run typecheck"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.2.5",
		"turbo": "^2.5.0",
		"typescript": "^5.8.2"
	},
	"packageManager": "pnpm@10.33.0",
	"engines": {
		"node": ">=20"
	}
}

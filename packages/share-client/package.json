{
	"name": "@opfs/share-client",
	"version": "0.1.0",
	"private": true,
	"description": "Page-side client for the recipient-first share rendezvous: HTTP transport + WASM seal/open orchestration (ADR 0007).",
	"license": "MIT",
	"type": "module",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./src/index.ts"
		}
	},
	"files": [
		"src"
	],
	"scripts": {
		"test": "vitest run",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@opfs/core-wasm": "workspace:*"
	},
	"devDependencies": {
		"@opfs/share-backend": "workspace:*",
		"typescript": "^5.8.2",
		"vitest": "^2.1.0"
	}
}
